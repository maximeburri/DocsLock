import { DeviceGroupPipePipe } from './devices-group-pipe.pipe';

describe('DevicesGroupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceGroupPipePipe();
    expect(pipe).toBeTruthy();
  });
});
