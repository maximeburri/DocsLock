import { DevicesSelectedPipe } from './devices-selected.pipe';

describe('DevicesSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new DevicesSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
